services:
  frontend:
    image: ${REGISTRY}/frontend:${TAG}
    ports:
      - "81:80"
  backend:
    image: ${REGISTRY}/backend:${TAG}
  postgres:
    volumes:
      - pg_data:/var/lib/postgresql/data_staging
